---
interface Props {
  as?: "p" | "h1" | "h2" | "span";
  class?: string;
}

const { as: Tag = "span", class: className } = Astro.props;
---

<Tag class:list={["mask-reveal", className]}>
  <slot />
</Tag>
